import Style from '@styles/aboutProjects.module.scss';
import { ReactElement } from 'react';

export default function AboutProjects(): ReactElement {
  return <section className={Style.container} id='projects'></section>;
}
